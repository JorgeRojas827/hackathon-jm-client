CREATE TABLE Productos (
    id_Product INT AUTO_INCREMENT PRIMARY_KEY,
    str_name VARCHAR(30) NOT NULL,
    str_img VARCHAR(30) NOT NULL,
    num_precio FLOAT NOT NULL,
)

CREATE TABLE

INSERT INTO Productos (name, img) VALUES 
("", ""),